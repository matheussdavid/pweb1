import { DadosTelefonicosPipe } from './dados-telefonicos.pipe';

describe('DadosTelefonicosPipe', () => {
  it('create an instance', () => {
    const pipe = new DadosTelefonicosPipe();
    expect(pipe).toBeTruthy();
  });
});
